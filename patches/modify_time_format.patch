diff --git a/feeds/luci/modules/luci-base/luasrc/view/admin_status/index.htm b/feeds/luci/modules/luci-base/luasrc/view/admin_status/index.htm
index 8f52b71..a1b3c4d 100644
--- a/feeds/luci/modules/luci-base/luasrc/view/admin_status/index.htm
+++ b/feeds/luci/modules/luci-base/luasrc/view/admin_status/index.htm
@@ -19,7 +19,7 @@
 					<tr><td width="33%"><%:System%></td><td><%=luci.sys.sysname()%> <%=luci.sys.sysversion()%></td></tr>
 					<tr><td><%:Kernel%></td><td><%=luci.sys.kernel()%></td></tr>
 					<tr><td><%:Hostname%></td><td><%=luci.sys.hostname()%></td></tr>
-					<tr><td><%:Local Time%></td><td id="localtime"><%=os.date()%></td></tr>
+					<tr><td><%:Local Time%></td><td id="localtime"><%=os.date("%Y/%m/%d %H:%M:%S")%></td></tr>
 					<tr><td><%:Uptime%></td><td id="uptime"><%=luci.sys.uptime()%></td></tr>
 					<tr><td><%:Load Average%></td><td id="loadavg"><%=table.concat(luci.sys.loadavg(), ", ")%></td></tr>
 				</table>
diff --git a/feeds/luci/modules/luci-base/htdocs/luci-static/resources/overview.js b/feeds/luci/modules/luci-base/htdocs/luci-static/resources/overview.js
index 6d78f90..e5a3b2c 100644
--- a/feeds/luci/modules/luci-base/htdocs/luci-static/resources/overview.js
+++ b/feeds/luci/modules/luci-base/htdocs/luci-static/resources/overview.js
@@ -32,7 +32,7 @@ 'use strict';
 	function updateLocalTime() {
 		var now = new Date();
 		var elem = document.getElementById('localtime');
-		elem.innerText = now.toLocaleString();
+		elem.innerText = now.getFullYear() + '/' + 
+			(now.getMonth() + 1).toString().padStart(2, '0') + '/' + 
+			now.getDate().toString().padStart(2, '0') + ' ' + 
+			now.getHours().toString().padStart(2, '0') + ':' + 
+			now.getMinutes().toString().padStart(2, '0') + ':' + 
+			now.getSeconds().toString().padStart(2, '0');
 	}
 
 	function updateUptime() {
